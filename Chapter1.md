## 🔹 Haskell — Функции и оператори (кратки бележки)

### 1️⃣ Оператори срещу функции

**Операторите** по подразбиране се използват **инфиксно**:  
  `haskell`
  2 + 3
  
Могат да се използват и като обикновени функции, ако се поставят в скоби:

(+) 2 3

Функциите по подразбиране се използват префиксно:

add 2 3

За да ги използваме инфиксно, ги поставяме в обратни кавички:

2 `add` 3

### 2️⃣ **Приоритет на свързване (binding precedence)**

Функциите имат по-висок приоритет на свързване от операторите.
Когато има неяснота дали даден аргумент принадлежи на функция или оператор, Haskell винаги избира функцията.

### 3️⃣ **Аргументи**

Една функция може да има произволен брой аргументи.
Един оператор винаги има точно два аргумента — затова ги наричаме двоични оператори.

### 4️⃣ **Правила за именуване**

Функциите се именуват с букви (add, map, sum).

Операторите се именуват със символи (+, >>=, ::).

### 5️⃣ **Конструктори**
Ако името на функция започва с главна буква, тя е конструктор на тип.

Операторите, които започват с двоеточие (:), също са запазени за конструктори на типове.

💡 *Пример:*

data Maybe a = Nothing | Just a
Тук Maybe е конструктор на тип, а Just е конструктор на данни.

## 🔹 Приоритет и асоциативност на операторите в Haskell

<img width="358" height="170" alt="image" src="https://github.com/user-attachments/assets/0b2ff903-a75b-49f2-8e3c-b2199020cb75" />


### 1️⃣ Проблемът с новия оператор

Проблемът е, че нашият нов оператор има **по-висок приоритет** от умножението.  
Затова в първия пример Haskell изчислява:

`haskell
(1 +++ 2) * 3`

В обикновената аритметика умножението има по-висок приоритет от събирането,
и същото важи и в Haskell — (*) има по-висок приоритет от (+).
Това означава, че във втория пример всъщност се изчислява:

`haskell
1 + (2 * 3)`

### 2️⃣ Решението — fixity декларация

Можем да поправим това, като добавим fixity декларация.

Тя определя асоциативността и приоритета на свързване (binding precedence) на даден оператор.

Fixity декларацията има три части:

a) Асоциативност

infixl — лява асоциативност (операторът се групира отляво)

infixr — дясна асоциативност (операторът се групира отдясно)

infix — без асоциативност (операторът не се групира автоматично)

b) Приоритет (binding precedence)

Представлява число от 0 (най-нисък приоритет) до 9 (най-висок приоритет).

Колкото по-високо е числото, толкова по-силно “залепва” операторът към своите аргументи.

c) Името на оператора

Това е самият оператор (или инфикс функцията), за който определяш fixity.

### 3️⃣ Пример: нормалното събиране

В Haskell обикновеното събиране (+) е:

лявоасоциативно

с приоритет 6

Можеш да провериш това в официалната документация
или направо в ghci, като напишеш:

λ :info (+)


Ще видиш нещо подобно на:

infixl 6 +


💡 Бележка:
Ако създаваш свой собствен оператор, винаги определяй fixity декларация,
за да избегнеш двусмислици при комбиниране с други оператори.






